# Project 7 — Dockerizing an AI Model API

This project demonstrates how to package and deploy a Machine Learning model as a fully containerized API using **Docker**.

## 🚀 Features
- FastAPI inference API
- Dockerfile for reproducible builds
- Local container testing
- Production-ready folder structure
- Can be deployed to Render, Railway, or AWS

## 🧠 Model
The model is a simple ML classifier trained in Project 6 (`model.pkl`).

## 📂 Project Structure
